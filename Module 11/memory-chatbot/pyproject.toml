[project]
name = "memory-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hariharan S", email = "hariharan@ideas2it.com" }
]
requires-python = ">=3.10"
dependencies = [
    "together>=2.32.0",
]

[project.scripts]
memory-chatbot = "memory_chatbot:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
