{
  "id": "repo-read",
  "name": "Repo Read",
  "description": "Read files from the configured repository and return line-numbered content.",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "required": [
      "repositoryPath"
    ],
    "properties": {
      "repositoryPath": {
        "type": "string",
        "description": "Absolute path to the repository that RepoGuide is allowed to read."
      }
    }
  },
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "repo_read"
    ]
  }
}
